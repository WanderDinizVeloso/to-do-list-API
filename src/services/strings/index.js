const USER = 'user';
const USERS = 'users';
const EMAIL = 'email';
const TOKEN = 'token';
const TOKENS = 'tokens';
const EMAIL_OR_PASSWORD = 'email or password';

module.exports = {
  USER,
  USERS,
  EMAIL,
  TOKEN,
  TOKENS,
  EMAIL_OR_PASSWORD,
};
