const toDoStatus = [
  'editável', 'pendente', 'em andamento', 'pronto',
];

const sortOrder = [
  'DESC', 'ASC',
];

const sortBy = [
  'message', 'updateAt', 'status', 'createdAt',
];

module.exports = {
  toDoStatus,
  sortOrder,
  sortBy,
};